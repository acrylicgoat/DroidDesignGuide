<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.acrylicgoat.droiddesign"
    android:versionCode="5"
    android:versionName="1.4" >

    <uses-sdk android:targetSdkVersion="17" android:minSdkVersion="10" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/ddg_logo"
        android:label="@string/app_name" 
        android:theme="@style/Theme.Sherlock" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" 
            android:theme="@style/Theme.Sherlock"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SublistActivity" android:label="@string/app_name" android:theme="@style/Theme.Sherlock"/>
        <activity android:name=".activity.DDGViewActivity" android:label="@string/app_name" android:configChanges="keyboardHidden|orientation" android:theme="@style/Theme.Sherlock"/>
    </application>

</manifest>